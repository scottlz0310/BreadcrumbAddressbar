[tool:pytest]
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short --cov=breadcrumb_addressbar --cov-report=term-missing
markers =
    qt: marks tests as requiring Qt (deselect with '-m "not qt"') 