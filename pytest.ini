[tool:pytest]
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short
markers =
    qt: marks tests as requiring Qt (deselect with '-m "not qt"')
# Qt APIを明示的に指定（CI環境での安定性向上）
qt_api = pyside6 